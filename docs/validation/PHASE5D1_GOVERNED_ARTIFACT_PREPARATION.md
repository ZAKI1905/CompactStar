# Phase-5D-1 governed-artifact preparation record

## Disposition and authenticated authority

**READY for a separate governed baseline-promotion / canonical-integration task.** The
controlled frozen-v1 science remains human-ratified. This task repaired the
reproducibility harness and prepared a deterministic promotion candidate; it did not
install a Phase-5D baseline, merge the candidate branch, begin realistic A18 work, or
begin BNV.

| Authority | Authenticated value |
|---|---|
| Canonical entry | `d019ae390be4f5e3daba05039903485cb497e397` |
| Ratification entry | `77fda93107b474e2e5420dde1bd84ec55762d727` |
| Artifact-preparation plan | `b53f26afeaa29e2e43744e27edda2cb5a94fa7c6` |
| Reproducibility implementation | `da1789ea152e2f0f3101f1ffecd54144e9d093d8` |
| Historical candidate | `phase5d1_controlled_evolution_candidate.json`, SHA-256 `47da15fa9e32be095a78d1b78ed17c3ffa14e5ef9be5528db3780a76afd0c079` |
| Promotion candidate | `phase5d1_controlled_evolution_promotion_candidate.json`, SHA-256 `0fc183202f9ca5651d0c600546cd0ca3893629b4db076c19d31fb34db84c21fa` |

The entry audit authenticated all ten governed baselines, including Phase-5B
`7588f0e9cd62f5b6be48bb725e4d0ba6e47b64f50d843117e70c17a78beeb5fa`
and Phase-5C
`7027aa6179fe9111d76586d467d007c4223ec9d26ece0fee773327973c77bfe7`,
plus the reviewed Phase-5C candidate
`a6b430b2356987b54a7c82c87d0b00f3e1d9698f6f299c32c80efcc40c63833b`.

## Repaired dependency boundary

The pre-repair audit found 29 hidden-state dependency nodes in the full historical
chain, seven of which were sufficient to break the tracked harness. It found 15
literal `ROOT/'build'` sites (nine tracked and six ignored), five additional
cwd-relative hard-coded build sites, two trusted saved-return-code uses at one source
site, one hard-coded candidate-worktree path, and one sibling scratch-build
dependency. The historical JSON contains 65 occurrences of its absolute worktree
string; the schema audit counted 108 absolute path occurrences across 38 logical
selectors.

The repaired entry point is `tests/rotochemical/fresh_context.py`. It requires an
explicit clean source root, a non-existing scratch root, and an authenticated external
EOS/data root. It configures and compiles below the scratch root, generates the
33-path entry manifest from the tracked frozen-context plan, regenerates the complete
EOS-8192/radial-80000 qualification ladder, constructs thermal files, runs response
and component suites, generates coupled oracles, executes the live Phase-5B and
Phase-5C governed regressions at the trajectory handshake, generates all six
trajectories, validates them, and writes the closed scientific artifact. No sibling
build directory is inspected.

`tests/rotochemical/qualified_suite.py` authorizes only actual subprocess return
codes. Saved `.rc` files are output receipts, never input authority. Run-start source
hashes are rechecked before and after scientific gates. Qualification, thermal,
executable, oracle, and trajectory outputs are owned by the current invocation.

## Producer authority and field classification

The schema audit classified 434 logical selectors: 31 `SCIENCE_EQUALITY`, 67
`SOURCE_PROVENANCE_EQUALITY`, 25 `FIXTURE_EQUALITY`, 32
`NUMERICAL_METHOD_EQUALITY`, 58 `EXECUTION_PROVENANCE_INFORMATIONAL`, 20
`GOVERNANCE_CLASSIFICATION`, 132 `DERIVED_DIAGNOSTIC_EQUALITY`, and 69
`NARRATIVE_DOCUMENTATION_ONLY`. The promotion artifact has 19 closed top-level
sections. Its producer-authoritative fields are generated from the current
executable/harness invocation. Narrative interpretation, review dispositions,
recommendations, caveat prose, commit prose, and free-form explanations remain only
in validation, ratification, or integration documents.

The two formerly postprocessed step-statistics fields are now direct
`NUMERICAL_METHOD_EQUALITY` output. The validator computes
`maximum_accepted_steps_per_checkpoint` and
`last10_accepted_steps_per_checkpoint` from successive differences of each complete
cumulative `.steps` table. Baseline maximum is 362 and refined maximum is 364. They
are never patched after producer completion.

Crossing diagnostics remain checkpoint-bracket/log-interpolation diagnostics; no
event localization was added. Aggregate and channel crossing times, quasi-steady
diagnostics, Jacobians, stiffness scales, and net-power cancellation are generated by
the producer/validator.

## Complete historical-to-promotion mapping

The mapping below accounts for every historical top-level key. “Documentation only”
means that prose or execution narration is intentionally absent from the governed
scientific JSON; any equality-bearing machine value in that historical container is
mapped explicitly.

| Historical key(s) | Promotion destination or treatment |
|---|---|
| `canonical`, `frozen_context_plan_sha`, `implementation_sha` | `source_provenance.canonical_sha`, `.frozen_context_plan_sha`, `.science_implementation_sha` |
| `branch`, `branch_lineage`, `execution_source_parity` | execution/governance documentation only; stable source identities are in `source_provenance` |
| `source_hashes` | equality-bearing production hashes in `source_provenance.scientific_production_source_hashes`; test-path churn excluded |
| `protected_entry`, `protected_gate` | `protected_upstream` with exact 33 paths, ten baselines, three special artifacts, entry SHA, and release equality |
| `fixture`, `initial`, `state_layout` | `fixture` |
| `normalizations`, `normalization_classification`, `controlled_ledger` | `scientific_contract`; exact normalizations also retained under `fixture` mapping where compared |
| `spin` | `spin_driver` with `P0_s=0.001`, run interval, and `physical_spin_interpretation=false` |
| `solver` | `solver.configuration` |
| `stiffness` | step data to `solver.step_statistics`; Jacobians/timescales to `numerical_diagnostics` |
| `Ltilde`, `semantic_W_I`, `semantic_Z`, `Ltilde_support_km` | `coefficient_authority` |
| `semantic_authorities` | fixed authority identifiers in `scientific_contract`, `coefficient_authority`, and producer contract; prose omitted |
| `thermal_source_hashes` | `thermal_authority.source_hashes` |
| `thermal_authorities` | exact formula identity to `thermal_authority.envelope_provenance`; other narrative prose omitted |
| `checkpoints` | `trajectory_checkpoints` |
| `final`, `ranges`, `ledger_residuals`, `power_convergence_absolute` | `trajectory_summary` |
| `ODE_convergence`, `initial_condition_convergence` | `convergence` |
| `quasi_steady` | `quasi_steady`; narrative `authority` member omitted |
| `incremental_root_crossings`, `full_root_crossings`, `aggregate_incremental_power_crossings`, `aggregate_full_power_crossings` | `thermal_sign_crossings`; narrative `meaning` members omitted |
| `physical_Ltilde_radial_comparison`, equality-bearing `net_power_cancellation_diagnostic` | `numerical_diagnostics` |
| `oracle_results`, equality-bearing `pretrajectory_oracle_results`, equality-bearing `preflight_mutation_inventory_coverage` | `oracles` and `suite_results` |
| `all_six_trajectories_raw_rc`, `trajectory_validator_raw_rc`, equality-bearing `complete_validation` values | `suite_results` |
| `classification`, `candidate_only`, `governed_baseline`, `protected_baseline_installed` | `classification` and the explicit no-baseline state |
| `run_provenance` | stable logical content hashes in `producer_provenance`; absolute paths/commands/executable details in execution sidecar |
| `validation_status`, `architecture_guards`, `archived_draft_audit`, `development_execution_notes`, `evidence_files`, `final_disposition`, `final_gate`, `final_test_only_supplement`, `future_work`, `implementation_team_reviews`, `reproduction_prerequisites`, `trajectory_figure`, `INV11_status`, `caveats` | documentation only, except equality-bearing values already mapped above |

The historical audit identified exactly 31 top-level additions not emitted by the old
validator: `validation_status`, `implementation_sha`, `branch_lineage`,
`execution_source_parity`, `semantic_authorities`, `thermal_authorities`,
`Ltilde_support_km`, `state_layout`, `protected_entry`, `archived_draft_audit`,
`oracle_results`, `architecture_guards`, `all_six_trajectories_raw_rc`,
`trajectory_validator_raw_rc`, `evidence_files`, `caveats`, `future_work`,
`aggregate_incremental_power_crossings`, `net_power_cancellation_diagnostic`,
`implementation_team_reviews`, `trajectory_figure`, `development_execution_notes`,
`reproduction_prerequisites`, `aggregate_full_power_crossings`,
`pretrajectory_oracle_results`, `preflight_mutation_inventory_coverage`,
`final_test_only_supplement`, `complete_validation`, `final_gate`, `INV11_status`,
and `final_disposition`.

## Execution provenance and governance fields

Each run emitted `execution-sidecar.json`. Between the two successful runs, the
informational differences were exactly `artifact_path`, `build_dir`, `commands`,
`entry_manifest_path`, `executable_path`, `executable_sha256`, `qualification_path`,
`scratch_root`, `source_root`, and `trajectory_path`. The executable hashes were
`9490de3a79e3a2010907153d40f2a1e08f9199540eb2ffafab53b484860c4ef1`
and `4ca2bfbd2b75d65663079eab8f1b4fb632c72121801371d1abbf1d5b29789c3f`;
they are retained honestly as path/toolchain-sensitive execution information, not
scientific equality. No absolute scratch path occurs in the promotion artifact.

The promotion classification is:

```text
classification = promotion_candidate
candidate_only = true
governed_baseline = false
benchmark_scope = CONTROLLED_MATHEMATICAL_ARCHITECTURE
physical_spin_interpretation = false
super_kepler = true
```

The old historical label remains unchanged in the ratified candidate:
`iron Potekhin1997 envelope`. The sole permitted future metadata correction is emitted
as `FR2005 Eq. (49) / PCY97 fully accreted`. The source formula, thermal bytes, and
every trajectory number are unchanged. The P0=1 ms fixture, mass/radius, spin history,
and run interval are unchanged and are explicitly mathematical rather than a physical
pulsar interpretation.

A later candidate-to-baseline transition may change only:

```text
classification: promotion_candidate -> governed
candidate_only: true -> false
governed_baseline: false -> true
```

The fail-closed comparator permits no scientific, fixture, source-provenance,
numerical-method, diagnostic, or other metadata difference.

## Fresh-run determinism and controls

| Evidence | Run 1 | Run 2 |
|---|---|---|
| Detached source | `/tmp/phase5d-repro-sources.Uv6mCQ/source-1` | `/tmp/phase5d-repro-sources.Uv6mCQ/source-2` |
| Scratch root | `/tmp/phase5d-repro-runs.142ItQ/run-1` | `/tmp/phase5d-repro-runs.142ItQ/run-2` |
| Source HEAD | `da1789ea152e2f0f3101f1ffecd54144e9d093d8` | same |
| Artifact SHA-256 | `0fc183202f9ca5651d0c600546cd0ca3893629b4db076c19d31fb34db84c21fa` | same |
| Configure/build/qualification/response/component/oracle/trajectory/validator rc | all 0 | all 0 |

The two producer-authoritative artifacts are byte-identical. All six trajectory TSVs,
baseline/refined step tables, baseline/refined Jacobian tables, and the transported
qualification certificate are byte-identical. Both runs regenerated exact target
hashes: freegas `7cd44c92e1e7206e0e68e3fed7e3f0ca68e79ab4517d02b96ff78b9be23d3f1a`,
profile `e9cd03b0b8449806f6c9883d75de1d3dff0cf1481675efc45a56519655d40890`,
model `3ea70de79e15b70c5a6d68f48335d18047ff80e60b55a9acdb78084e9be4d6d4`,
and certificate `7fc892b50bfbcdd2c5d963e0ff866777f3628f40fc282e1ce5a0b0364200a453`.

Negative controls passed:

1. A fake sibling build with a fake certificate and saved rc=0 was ignored.
2. A forged saved rc=0 beside an actual rc=9 command was refused; rc=9 was recorded.
3. Missing qualification producer output was refused.
4. An actual protected source mutated after entry-manifest generation was refused.
5. A thermal source mutated after construction was refused.
6. Run 1 generated completely with the historical candidate physically withheld.
7. Both detached repro sources lacked any promotion candidate and generated successfully.
8. Alternate absolute scratch roots yielded byte-identical scientific artifacts.

The producer does not read the historical candidate, a promotion candidate, or any
Phase-5D baseline while constructing results. The historical file is accessed only by
the post-generation comparator.

## Scientific equality and validation

`tests/rotochemical/compare_artifacts.py historical-promotion` passed. The projections
agree for fixture/normalizations/state layout/spin, production-source hashes, Z, W, I,
Ltilde and supports, thermal bytes, oracles, all checkpoints and final state, ODE and
initial-condition convergence, quasi-steady diagnostics, all thermal-sign crossings,
step/Jacobian/stiffness/cancellation diagnostics, protected-source state, and suite
science fields. The only value-level metadata correction is the predeclared envelope
description. There are no unexpected differences and no numerical effect.

Validation completed with actual command results:

| Gate | Result |
|---|---|
| New harness unit/negative tests | PASS, raw rc 0 |
| Retained response suite | PASS, 2/2, raw rc 0 |
| Retained component-tolerance suite | PASS, raw rc 0 |
| Fresh coupled suite | PASS, raw rc 0 |
| Fresh six-trajectory producer and validator | PASS, raw rc 0 |
| Phase-5B governed regression | PASS, raw rc 0 |
| Phase-5C governed regression | PASS, raw rc 0 |
| Complete data-free suite | PASS, 51/51, raw rc 0 |
| Complete authenticated suite | PASS, 74/74, raw rc 0 |

There were zero failures and zero unexplained skips. The historical ratified candidate,
all ten governed baselines, reviewed Phase-5C candidate, all 33 protected upstream
paths, EOS/data authority, and literature authority remain unchanged. No scientific
production source changed; the only C++ diff is the non-numerical test-harness metadata
label in `tests/rotochemical/trajectory.hpp`.

## Remaining scope and caveats

No Phase-5D baseline is installed and canonical `master` is not changed. Global INV-11
remains unresolved. The ratified caveats remain unchanged: Lother/PBF-only interface,
RKF45 stability-bound behavior, heat-capacity cache kinks, Sommerfeld/free-gas
limitations, omitted dot(Z), DU, and superfluidity, the super-Kepler P0=1 ms
mathematical control, and realistic A18/FR2005 source limitations. A18 and BNV were not
begun.

The promotion candidate is **PREPARED / NOT YET GOVERNED** and the reproducibility
harness is **REPAIRED / FRESH-CONTEXT VALIDATED**. Canonical integration remains
pending.
